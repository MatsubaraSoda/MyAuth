{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev --persist-to ../../.wrangler/state",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "dev:drizzle-kit": "npx drizzle-kit studio --config drizzle.config.ts"
  },
  "dependencies": {
    "better-auth": "^1.6.2",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260413.1",
    "@libsql/client": "^0.17.2",
    "drizzle-kit": "^0.31.10",
    "wrangler": "^4.4.0"
  }
}
